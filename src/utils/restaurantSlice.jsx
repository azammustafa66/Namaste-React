import { createSlice } from "@reduxjs/toolkit";

const restaurantSlice = createSlice({
    name: 'restaurant name in cart',
    initialState: {
        restaurants: [],
    },
    reducers: {
        
    }
})